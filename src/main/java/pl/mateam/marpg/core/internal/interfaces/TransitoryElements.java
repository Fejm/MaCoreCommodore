package pl.mateam.marpg.core.internal.interfaces;

//TransitoryElements should contain methods, which assigns values to its NullAtRuntime fields, so it can be saved to database
public interface TransitoryElements {}
